from setuptools import setup

setup(name='mlflow_analytics',
      version='0.0.1',
      description='Create Spark tables from MLflow experiments and runs',
      author='Andre',
      packages=['mlflow_analytics'],
      zip_safe=False)
