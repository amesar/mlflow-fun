from setuptools import setup

setup(name='mlflow_fun_common',
      version='0.0.1',
      description='MLflowFun common',
      author='Andre',
      packages=['mlflow_fun',
                'mlflow_fun.common'],
      zip_safe=False)
